.status__content><
  - if status.spoiler_text.present?
    %details<
      %summary><
        %strong> Content warning: #{prerender_custom_emojis(h(status.spoiler_text), status.emojis + status.avatar_emojis)}
      = prerender_custom_emojis(status_content_format(status), status.emojis + status.avatar_emojis)
      = render partial: 'admin/shared/status_attachments', locals: { status: status.proper }
  - else
    = prerender_custom_emojis(status_content_format(status), status.emojis + status.avatar_emojis)
    = render partial: 'admin/shared/status_attachments', locals: { status: status.proper }
